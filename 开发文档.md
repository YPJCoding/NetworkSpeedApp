# NetworkSpeedApp 开发文档

> 文档版本：v2（结合开源项目调研修订）

## 1. 项目概述

NetworkSpeedApp 是一款原生 macOS 状态栏网速监控工具，用于实时显示当前设备的上传与下载速度。

应用采用 Swift、SwiftUI 和 Swift Package Manager 开发，不依赖第三方库，不需要管理员权限，不读取网络内容，仅统计系统网络接口的收发字节数。

### 1.1 核心目标

- 在 macOS 状态栏实时显示上传和下载速度
- 点击状态栏后查看网络状态并调整基础设置
- 汇总所有活动的物理网卡，兼容 VPN、Clash TUN 和系统代理
- 支持开机启动
- 保持低 CPU、低内存和低能耗
- 无需完整 Xcode 工程即可构建

### 1.2 首版非目标

以下功能不纳入首个版本：

- 按进程统计流量
- 按域名或远程地址统计流量
- 抓包或分析网络内容
- 云端同步
- 历史数据库及长期流量报表
- App Store 沙盒版本
- 自定义皮肤和复杂图表

## 2. 系统要求

- macOS 13 或更高版本
- Swift 6 工具链
- Apple Command Line Tools
- Apple Silicon 与 Intel Mac

首版以 Universal 2 应用为发布目标。

## 3. 技术栈

- Swift 6
- SwiftUI
- Combine（`ObservableObject`、`@Published`）
- Swift Concurrency（`async/await`、`Task`）
- Darwin `sysctl` / `NET_RT_IFLIST2`
- Network `NWPathMonitor`
- ServiceManagement `SMAppService`
- Swift Package Manager
- shell/Makefile 应用打包

## 4. 产品形态

应用使用 SwiftUI `MenuBarExtra`，不显示 Dock 图标，也不创建常驻主窗口。

### 4.1 状态栏展示

默认以双行形式显示：

```text
↑ 1.2 MB/s
↓ 8.5 MB/s
```

`MenuBarExtra` 的 SwiftUI 标签不能可靠布局多行 `VStack`，因此先使用 AppKit 将两行等宽文字绘制为 61×22pt 的模板 `NSImage`，再由 `MenuBarExtra` 显示图片。图标使用系统模板着色，由 macOS 根据菜单栏外观自动调整颜色。渲染结果按文字缓存，避免重复绘制。箭头列与“数字 + 单位”列使用固定位置和固定宽度，速度值在字段内右对齐；较大数值减少小数位数以保持紧凑，并防止单位或位数变化导致箭头左右跳动。

要求：

- 上传在上，下载在下
- 数字使用等宽字体，降低宽度抖动
- 单位自动在 `KB/s`、`MB/s`、`GB/s` 之间切换，低于 `10 KB/s` 时显示 `0 KB/s`
- 数值最多保留一位小数
- 采样异常时保留最近一次有效结果，并在详情中显示状态

### 4.2 详情菜单

使用 `MenuBarExtra` 的 `.menu` 样式，展开为 macOS 原生菜单列表项，不使用独立浮动面板或自定义固定宽度布局。网速每秒发布会导致状态模型更新，因此应用根视图只用 `@State` 持有模型，菜单视图只订阅刷新频率绑定和登录项状态，避免网速变化重建菜单并收起 Picker 子菜单。点击状态栏图标后显示：

- 刷新频率选择
- “登录时启动”开关
- 打开 GitHub 仓库链接
- “退出”操作

## 5. 开源项目调研结论

开发前重点参考以下项目：

| 项目 | 特点 | 可借鉴内容 |
|---|---|---|
| [elegracer/NetSpeedMonitor](https://github.com/elegracer/NetSpeedMonitor) | 专用网速菜单栏应用，MIT | `NET_RT_IFLIST2` 64 位计数器、逐接口保存基线、睡眠唤醒处理 |
| [exelban/stats](https://github.com/exelban/stats) | 成熟的综合系统监控器，MIT | 按接口读取数据、网络详情展示、累计流量重置策略；按进程统计成本较高，不适合首版常驻启用 |
| [mh-sudo/NetBar](https://github.com/mh-sudo/NetBar) | 专用网速与流量统计工具，MIT | 手动锁定接口、VPN 变化检测、采集逻辑集中到单一 Reader |
| [seppowalther/netfluss](https://github.com/seppowalther/netfluss) | SwiftUI 多网卡监控，GPL-3.0 | 每个接口独立计算速率、实际采样时间差、菜单栏固定宽度显示思路；仅借鉴设计，不复制 GPL 代码 |

调研后的关键结论：

1. `getifaddrs` 中传统 `if_data` 字节字段可能面临较短周期的计数器回绕；高带宽场景应优先使用 `sysctl + NET_RT_IFLIST2 + if_msghdr2` 提供的 64 位计数器。
2. 不能先把所有接口累计值相加再求差。必须按接口分别保存基线、分别计算有效增量，再汇总活动物理网卡的增量。
3. 只汇总 BSD 名称为 `en*` 的物理网卡，排除 `lo0`、`utun*`、bridge 和虚拟接口；这样可统计 Clash 系统代理外发流量，同时避免与 TUN 逻辑接口重复计数。
4. `NWPathMonitor` 适合判断连通状态和提示路径变化，但不提供字节计数，不能替代内核接口计数器。
5. 睡眠、唤醒、接口切换和计数器回退都应触发基线重建，不能把重置后的当前累计值算作一个采样周期的新增流量。
6. 按进程网速通常依赖 `nettop` 或类似命令，资源消耗和解析复杂度明显更高，不纳入首版常驻功能。

## 6. 总体架构

项目采用轻量分层 MVVM：

```text
系统网络接口计数器
        ↓
NetworkTrafficMonitor（采集层）
        ↓
NetworkSpeedModel（状态与业务逻辑）
        ↓
MenuBarLabel / DetailView（视图层）
```

### 6.1 设计原则

- View 只负责展示和用户交互
- 状态计算集中在 `NetworkSpeedModel`
- 系统接口访问集中在 `NetworkTrafficMonitor`
- 数据结构不依赖 SwiftUI
- 不为首版功能引入额外抽象
- 所有 UI 状态更新均在主 Actor 执行

## 7. 目录规划

```text
NetworkSpeedApp/
├─ Package.swift
├─ Makefile
├─ README.md
├─ 开发文档.md
├─ App/
│  ├─ Info.plist
│  ├─ build.sh
│  └─ Resources/
│     └─ AppIcon.icns
├─ scripts/
│  ├─ package-release.sh
│  └─ generate-icon.swift
├─ Sources/
│  └─ NetworkSpeedApp/
│     ├─ App.swift
│     ├─ Models/
│     │  └─ NetworkTraffic.swift
│     ├─ Services/
│     │  ├─ NetworkTrafficMonitor.swift
│     │  ├─ DefaultRouteResolver.swift
│     │  ├─ NetworkPathObserver.swift
│     │  └─ LoginItemManager.swift
│     ├─ State/
│     │  └─ NetworkSpeedModel.swift
│     └─ Views/
│        ├─ MenuBarLabel.swift
│        └─ DetailView.swift
└─ Tests/
   └─ NetworkSpeedAppTests/
      └─ NetworkSpeedCalculatorTests.swift
```

## 8. 模块设计

### 8.1 `App.swift`

职责：

- 应用入口
- 创建并持有 `NetworkSpeedModel`
- 配置 `MenuBarExtra`
- 在应用启动和退出时控制监控任务生命周期

### 8.2 `NetworkTraffic.swift`

定义不依赖 UI 的数据模型。

建议模型：

```swift
struct InterfaceTraffic: Sendable, Equatable {
    let name: String
    let receivedBytes: UInt64
    let sentBytes: UInt64
    let isUp: Bool
}

struct NetworkTrafficSnapshot: Sendable, Equatable {
    let interfaces: [String: InterfaceTraffic]
    let timestamp: ContinuousClock.Instant
}

struct NetworkSpeed: Sendable, Equatable {
    let downloadBytesPerSecond: Double
    let uploadBytesPerSecond: Double
}
```

### 8.3 `NetworkTrafficMonitor.swift`

职责：

- 通过 `sysctl` 读取 `NET_RT_IFLIST2`
- 安全遍历可变长度路由消息，并只处理 `RTM_IFINFO2`
- 从 `if_msghdr2.ifm_data` 读取 64 位收发字节计数器
- 仅保留 `en*` 物理网卡，并记录接口的 up/running 状态
- 每次采样只访问一次系统接口，返回按 BSD 接口名索引的不可变快照

读取缓冲区时必须校验消息长度、剩余缓冲区长度和消息类型，避免直接进行不安全的越界指针转换。采集层只保留 `en*` 物理网卡，并记录接口的 up/running 状态。

该服务不负责：

- 更新 SwiftUI 状态
- 格式化速度文本
- 保存用户设置
- 控制刷新定时器

### 8.4 网络接口筛选策略

`NetworkInterfacePolicy` 只将 BSD 名称以 `en` 开头的接口视为物理网卡。采集快照及速度计算只使用这些接口；VPN 隧道、回环、bridge、虚拟机及其他虚拟接口不参与汇总。

### 8.5 `NetworkPathObserver.swift`

职责：

- 使用 `NWPathMonitor` 观察在线状态及路径变化
- 仅在网络连通状态实际改变时通知状态层重建采样基线
- 不参与流量计数

### 8.6 `NetworkSpeedModel.swift`

职责：

- 定时执行采样
- 比较相邻快照并逐接口计算物理网卡流量增量
- 汇总所有活动物理网卡的上传和下载速度
- 处理网卡变化及计数器回退
- 管理刷新频率
- 向视图提供格式化后的显示数据
- 控制开始、停止与重新采样

为兼容 macOS 13，使用 Combine 的 `ObservableObject`、`@Published` 与 `@MainActor`（Observation 的 `@Observable` 需要 macOS 14）：

```swift
@MainActor
final class NetworkSpeedModel: ObservableObject {
    @Published private(set) var speed: NetworkSpeed
    @Published var refreshInterval: Double
}
```

### 8.7 `LoginItemManager.swift`

职责：

- 读取当前登录项状态
- 使用 `SMAppService.mainApp` 注册或取消注册
- 将系统错误转换为可显示的信息

### 8.8 `MenuBarLabel.swift`

职责：

- 展示上传和下载速度
- 使用紧凑布局和等宽数字
- 提供无障碍标签
- 不直接访问网络接口

### 8.9 `DetailView.swift`

职责：

- 提供刷新频率及开机启动设置
- 提供重置、关于和退出操作

## 9. 网速计算

### 9.1 基本公式

```text
下载速度 =（当前接收字节 - 上次接收字节）÷ 实际时间间隔
上传速度 =（当前发送字节 - 上次发送字节）÷ 实际时间间隔
```

不能假定定时器每次都精确间隔一秒，必须使用两次采样的实际时间差。状态栏显示最近 3 秒速度的时间加权平均，平滑短暂的零增量采样；持续无流量 3 秒后回到零。

### 9.2 首次采样

首次采样只能建立基线，速度显示为零；从第二次有效采样开始计算网速。

### 9.3 按接口计算与异常处理

必须按接口名匹配前后快照并分别计算差值，再汇总活动物理网卡的有效增量：

- 新出现的接口：本次只建立基线，不产生速度。
- 消失的接口：从基线表删除，不影响其他接口。
- 当前计数小于旧计数：视为接口重建或计数器重置，本次差值为零。
- 接口由 down 变为 up：重新建立该接口基线。
- 网卡新增、移除或连接状态变化：只重建受影响网卡的基线，稳定网卡继续计速。
- 睡眠唤醒：丢弃跨睡眠周期的差值，重新采样。
- 实际时间间隔小于 0.05 秒或明显超过预期：不计算速度，避免调度抖动或挂起造成误导。

仅汇总当前和上次快照都存在、且处于运行状态的 `en*` 物理网卡。任何单个接口异常都不应使其他稳定接口的基线失效。

## 10. 物理网卡筛选与聚合策略

应用逐个统计所有活动物理网卡，并将有效增量相加。物理网卡按 BSD 名称 `en*` 识别；`lo0`、`utun*`、bridge、AWDL 及虚拟机/容器接口均排除。Clash 使用 TUN 或系统代理时，外发网络流量最终经过物理网卡，因此状态栏仍能反映实际上传和下载活动，而不会把隧道与底层网卡重复相加。

每个接口独立保存采样基线。新出现的网卡首个周期只建立基线；某个网卡计数器回退时只忽略该接口本周期增量，不影响其他稳定网卡。

重点验证 Wi-Fi、有线及 USB 网卡同时活动、Clash TUN/系统代理、常规 VPN，以及网卡连接和断开。

## 11. 刷新与并发

默认刷新间隔为 1 秒，可提供：

- 0.5 秒
- 1 秒
- 2 秒
- 5 秒

建议使用可取消的 `Task` 循环和 `ContinuousClock`，而不是永久运行的 Timer 或墙上时钟 `Date`：

```text
启动监控
  ↓
采样并更新状态
  ↓
休眠指定时间
  ↓
检查 Task 是否取消
  ↓
继续下一次采样
```

要求：

- 同一时间只能存在一个监控任务
- 修改刷新频率后重启采样循环
- 应用退出时取消任务
- 采集可在后台执行，UI 状态在主 Actor 更新

## 12. 数据格式化

速度按 1024 进制格式化：

- 小于 1 MiB/s：`KB/s`（包括不足 1 KiB/s）
- 小于 1 GiB/s：`MB/s`
- 其他：`GB/s`

格式规则：低于 `10 KB/s` 显示 `0 KB/s`；其余速度最多保留一位小数，大数值省略小数位。

示例：

- `9 KB/s` → `0 KB/s`
- `10 KB/s` → `10.0 KB/s`
- `856 KB/s` → `856 KB/s`
- `8.42 MB/s` → `8.4 MB/s`
- `1.08 GB/s` → `1.1 GB/s`

状态栏使用固定宽度的箭头列和速度值列，并限制小数位数，避免速度变化导致布局跳动。

## 13. 设置持久化

使用 `UserDefaults` 保存：

- 刷新频率

开机启动状态以 `SMAppService` 的实际状态为准，不额外保存一个可能失真的布尔值。

## 14. 权限与隐私

应用只读取系统提供的网络接口累计计数，不读取数据包内容，因此：

- 不需要管理员权限
- 不需要辅助功能权限
- 不需要网络扩展权限
- 不需要抓包权限
- 不记录访问的网站、地址或网络内容

README 和“关于”页面应明确说明上述隐私边界。

## 15. 构建与发布

建议命令：

```text
make build    构建 .app
make run      构建并运行
make test     执行单元测试
make verify   验证应用结构、架构和签名
make dmg      生成 DMG 安装镜像
make release  生成 ZIP 和 DMG 发布包
make icon     重新生成图标
```

构建产物：

```text
build/Network Speed.app
```

发布产物：

```text
dist/Network-Speed-<version>-macOS-universal.zip
dist/Network-Speed-<version>-macOS-universal.zip.sha256
dist/Network-Speed-<version>-macOS-universal.dmg
dist/Network-Speed-<version>-macOS-universal.dmg.sha256
```

DMG 内含应用和指向 `/Applications` 的快捷方式，支持拖放安装。`make dmg` 只生成 DMG，`make release` 同时生成 ZIP 与 DMG。

本地开发使用 ad-hoc 签名；公开发布时使用 Developer ID 签名并完成 Apple 公证。

`Info.plist` 至少包含：

- `CFBundleIdentifier`
- `CFBundleName`
- `CFBundleDisplayName`
- `CFBundleExecutable`
- `CFBundleShortVersionString`
- `CFBundleVersion`
- `LSMinimumSystemVersion`
- `LSUIElement = true`

## 16. 测试计划

### 16.1 单元测试

必须覆盖：

- 正常字节增量计算
- 使用实际时间间隔计算速度
- 首次采样返回零速度
- 64 位大计数值的正常增量
- 单个接口计数器回退时只重建该接口基线
- 接口新增、消失和 down/up 切换
- 网卡新增或移除时只重建对应网卡基线
- 某个网卡计数器回退时其他网卡仍正确计速
- 多物理网卡增量汇总正确，utun/lo0/bridge 不参与统计
- 零时间、过短时间和跨睡眠异常间隔处理
- KB、MB、GB 单位边界

为便于测试，速度计算应是无副作用的纯逻辑，不直接依赖 `sysctl` 或其他系统 API。

### 16.2 手动测试

- Wi-Fi 下载大文件时下载速度上升
- 上传文件时上传速度上升
- 断开并重新连接 Wi-Fi 后无异常峰值
- 在 Wi-Fi 与有线网络之间切换
- Clash TUN、系统代理及 VPN 流量由物理网卡统计，隧道接口不重复计入
- 使用超过 4 GiB 的连续传输验证无 32 位回绕
- 睡眠唤醒后恢复刷新且不计入跨睡眠差值
- 修改刷新频率后不存在重复采样任务
- 登录项注册和取消正常
- 退出应用后无残留进程
- 状态栏文本不会明显抖动
- VoiceOver 能正确读取上传和下载速度

### 16.3 性能目标

空闲状态下建议达到：

- CPU 平均占用低于 1%
- 内存占用低于 50 MB
- 每个刷新周期只遍历一次网络接口
- 不产生持续磁盘写入

## 17. 异常处理

| 场景 | 处理方式 |
|---|---|
| 无有效网络接口 | 速度归零，状态显示未连接 |
| `sysctl` 读取或消息解析失败 | 当前周期速度归零并记录可恢复错误 |
| 网络路径发生变化 | 清零瞬时速度并重建所有物理网卡基线 |
| 接口新增或消失 | 只重建受影响接口的基线 |
| 计数器回退 | 丢弃该接口本周期差值并重建基线 |
| 系统睡眠或异常长采样间隔 | 丢弃跨越该时段的差值并重建全部基线 |
| 监控任务被取消 | 正常结束，不显示错误 |
| 登录项注册失败 | 在菜单中展示明确错误信息 |

日志中不得高频输出每次采样结果。只记录生命周期、配置变化和异常事件。

## 18. 开发阶段

开发分为五个阶段。每个阶段必须先通过对应验证，再进入下一阶段；底层采集和计算引擎未验证前，不提前完善界面。

### 阶段一：底层采集原型

实现：

- 创建 Swift Package
- 使用 `sysctl + NET_RT_IFLIST2` 获取网络接口数据
- 安全遍历可变长度路由消息
- 读取各接口的 64 位收发字节计数
- 输出接口名、运行状态和累计流量

验证：

- 能统计 Wi-Fi、有线及 USB 物理网卡；不将 VPN/TUN、bridge 或虚拟接口纳入结果
- 连续传输超过 4 GiB 时不发生计数回绕
- 异常路由消息不会造成越界或崩溃
- 多次采样不存在明显内存泄漏

本阶段只验证底层能力，不开发正式界面。

### 阶段二：网速计算引擎

实现：

- 按接口保存采样基线并使用实际时间间隔计算速度
- 识别并采集所有物理网卡（`en*`）
- 对各网卡独立建立基线并汇总有效流量增量
- 处理接口新增、消失、down/up 和计数器回退
- 处理睡眠唤醒、网卡变化和异常采样间隔
- 将速度计算实现为不依赖系统 API 的纯逻辑
- 编写核心单元测试

验证：

- 上传和下载方向正确，数量级合理
- Wi-Fi 与有线网络切换不产生异常峰值
- Clash TUN/系统代理及 VPN 场景不将隧道流量与物理网卡重复计数
- 所有计算单元测试通过

### 阶段三：状态栏最小可用版本

实现：

- 使用 SwiftUI `MenuBarExtra` 创建状态栏应用
- 使用系统模板图片双行显示上传和下载速度
- 自动切换速度单位并使用等宽数字
- 增加只包含基础设置和应用操作的精简详情菜单
- 设置 `LSUIElement = true`，隐藏 Dock 图标

验证：

- 应用可以连续运行数小时并稳定刷新
- 状态栏宽度不会明显抖动
- 空闲 CPU 平均占用低于 1%，内存占用低于 50 MB
- VoiceOver 能正确读取上传和下载速度

完成本阶段后形成可日常使用的 MVP。

### 阶段四：设置与系统集成

实现：

- 增加刷新频率设置
- 使用 `UserDefaults` 持久化设置
- 使用 `NWPathMonitor` 感知网络路径变化
- 使用 `SMAppService` 实现登录时启动
- 增加关于和退出操作
- 增加应用图标

验证：

- 重启应用后设置正确恢复
- 登录项可以正常注册和取消
- 修改刷新频率不会创建重复监控任务
- 断网、重连和睡眠唤醒后自动恢复监控

### 阶段五：打包与发布

实现：

- 完成 `Makefile`、`.app`、ZIP 与 DMG 打包脚本
- 构建 Universal 2 应用
- 增加应用结构、架构和签名验证
- 完成 Developer ID 签名与 Apple 公证
- 生成发布压缩包和 SHA-256
- 编写用户 README 和隐私说明

验证：

```text
make build
make test
make verify
make release
```

同时在 Apple Silicon、Intel、Wi-Fi、有线网络、VPN、无网络及睡眠唤醒场景下完成安装和运行验证。

## 19. 验收标准

首版完成需同时满足：

1. 应用在状态栏持续显示上传和下载速度。
2. Wi-Fi、有线、VPN 及 Clash TUN/系统代理场景的统计结果方向正确、数量级合理。
3. 网络断开、重连、睡眠唤醒及接口切换不会产生异常大值。
4. 所有活动物理网卡均被统计，VPN/TUN/虚拟接口被排除，不发生重复计数。
5. 详情菜单只显示刷新频率、登录启动和应用操作。
6. 用户可以修改刷新频率并控制登录时启动。
7. 应用不显示 Dock 图标，不需要管理员权限。
8. 核心计算逻辑具备单元测试且全部通过。
9. `make build`、`make test` 和 `make verify` 均成功。
10. 构建产物可脱离源码目录独立运行。
11. 空闲状态下 CPU 和内存占用符合性能目标。
