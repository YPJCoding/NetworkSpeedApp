# 隐私说明

网络速度完全在本机运行。

应用会读取 macOS 网络接口的累计上传和下载字节计数，用于计算实时速度与本次运行累计流量。应用不会：

- 抓取或检查网络数据包内容；
- 记录访问的网站、域名、IP 地址或应用流量；
- 将数据发送到开发者或第三方服务；
- 使用分析、广告或遥测 SDK。

刷新频率和用户选择的接口保存在本机 `UserDefaults` 中。本次运行累计流量在退出后不会保留。
